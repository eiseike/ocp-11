package chapter.p.XVI.Exceptions;

public class Asserts {
    public static void main(String[] args) {
//        assert(1); //DNC:  int cannot be converted to boolean
//        assert x -> true; //DNC: boolean is not a functional interface
//        assert 1==2 ? "Accept" : "Error"; //DNC: bad type in conditional expression
//        assert.test(5> age); //DNC: illegal start of expression

        assert false;
    }
}
